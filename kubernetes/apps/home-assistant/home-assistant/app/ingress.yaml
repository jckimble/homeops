---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: home-assistant
  annotations:
    tailscale.com/hostname: hass
spec:
  ingressClassName: tailscale
  tls:
    - hosts:
        - hass
  defaultBackend:
    service:
      name: home-assistant
      port:
        number: 8123
