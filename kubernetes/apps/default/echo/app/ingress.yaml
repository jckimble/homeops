---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: echo
  annotations:
    tailscale.com/hostname: echo
spec:
  ingressClassName: tailscale
  tls:
    - hosts:
        - echo
  defaultBackend:
    service:
      name: echo
      port:
        number: 80
